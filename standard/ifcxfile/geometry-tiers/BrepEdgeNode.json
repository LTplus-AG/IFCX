{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "BrepEdgeNode.json",
    "type": "object",
    "properties": {
        "Curve": {
            "$ref": "BrepCurve.json",
            "description": "Geometric carrier of the edge, inline (the edge *is* this curve)."
        },
        "Start": {
            "$ref": "#/$defs/BrepRef",
            "description": "Reference to the start vertex node"
        },
        "End": {
            "$ref": "#/$defs/BrepRef",
            "description": "Reference to the end vertex node"
        }
    },
    "required": [
        "Curve",
        "Start",
        "End"
    ],
    "description": "Edge node body — a curve bounded by two vertex references.",
    "$defs": {
        "BrepRef": {
            "type": "string",
            "pattern": "^<[^>]+>$",
            "description": "Relative- or absolute-path reference to another Brep topology node, wrapped in\nangle brackets (USD relationship-target syntax). `../` ascends one segment\nrelative to the referencing node's composed path; a leading `/` is absolute.\nExamples: \"<../Edge_3>\", \"<../Vertex_0>\"."
        }
    }
}